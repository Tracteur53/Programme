<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jpet_store">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jpet_Store">
        <activity android:name=".PaymentActivity"></activity>
        <activity
            android:name=".SignIn2_Activity"
            android:label="@string/title_activity_sign_in2_"
            android:theme="@style/Theme.Jpet_Store.NoActionBar" />
        <activity android:name=".SignIn_Activity" />
        <activity android:name=".Login_Activity" />
        <activity android:name=".Card" />
        <activity android:name=".PageType" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Jpet_Store.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AngelFish" />
    </application>

</manifest>